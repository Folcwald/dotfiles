# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
	output_format = "i3bar"
        colors = true
        interval = 5
}

order += "tztime local"
# order += "disk /home"
# order += "run_watch DHCP"
# order += "ethernet eno1"
# order += "cpu_usage"
# order += "cpu_temperature 0"
# order += "gpu_temperature 0"
# order += "load"
# order += "volume master"
order += "tztime berlin"

#ethernet eno1 {
#        # if you use %speed, i3status requires root privileges
#        format_up = "E: %ip up (%speed)"
#        format_down = "E: %ip down (%speed)"
#}

# cpu_usage {
#	format = "%usage"
# }	

# cpu_temperature 0 {
#	format = "CPU-T: %degrees °C"
#        path = "/sys/devices/platform/coretemp.0/temp2_input"
#}

#gpu_temperature 0 {
#	format = "GPU-T: %degrees °C"
#	exec = "nvidia-settings -t -q gpucoretemp"
#}

#run_watch DHCP {
#        pidfile = "/var/run/dhclient*.pid"
#}

tztime berlin {
	format = "%I:%M %p"
	timezone = "Europe/Berlin"
}

tztime local {
        format = "%a %d %b %I:%M %p"
}

#load {
#        format = "%1min"
#}

#disk /home {
#        format = "%avail"
#}

#volume master {
#      format = "♪: %volume"
#      format_muted = "♪: muted (%volume)"
#      device = "default"
#      mixer = "Master"
#      mixer_idx = 0
#}
